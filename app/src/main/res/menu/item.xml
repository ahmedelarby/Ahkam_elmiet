<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<item android:id="@+id/item1"
    app:showAsAction="always"
    android:icon="@drawable/ic_home"
    android:title="القائمه الرئسيه"/>

    <item android:title="القائمه السابفه"
        android:id="@+id/item2"/>

    <item android:id="@+id/plas"
        android:icon="@drawable/ic_zoom_in_"
        app:showAsAction="always"
        tools:ignore="MenuTitle" />

    <item android:icon="@drawable/ic_zoom_out"
        app:showAsAction="always"
        android:id="@+id/mainase"
        tools:ignore="MenuTitle" />

<item android:id="@+id/finsha"
    android:title="خروج"/>
</menu>